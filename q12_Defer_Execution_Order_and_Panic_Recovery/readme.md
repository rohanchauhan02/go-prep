# Q12: Defer Execution Order and Panic Recovery

## Problem Statement
Write a function with multiple defers showing LIFO order. Add recover() inside a deferred function to catch a panic and return it as an error. Show defer with named return value modification.

## Key Concepts
```
defer LIFO, recover(), named return values, panic propagation
```

## Requirements
- Solve the problem in `main.go`
- Include example output in comments
- Add unit tests where applicable

## How to Run
```bash
go run main.go
```
