# Q27: Trie Data Structure

## Problem Statement
Implement a Trie (prefix tree) with Insert, Search, StartsWith, and Delete methods. Use it to implement an autocomplete function returning all words with a given prefix.

## Key Concepts
```
struct with children map, recursive delete, BFS for autocomplete
```

## Requirements
- Solve the problem in `main.go`
- Include example output in comments
- Add unit tests where applicable

## How to Run
```bash
go run main.go
```
