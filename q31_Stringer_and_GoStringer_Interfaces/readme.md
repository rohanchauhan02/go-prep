# Q31: Stringer and GoStringer Interfaces

## Problem Statement
Implement fmt.Stringer and fmt.GoStringer on a custom Color type (RGBA fields). Show how fmt.Sprintf %v, %+v, %#v use these interfaces differently.

## Key Concepts
```
String() string, GoString() string, fmt verbs %v %+v %#v
```

## Requirements
- Solve the problem in `main.go`
- Include example output in comments
- Add unit tests where applicable

## How to Run
```bash
go run main.go
```
