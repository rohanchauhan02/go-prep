# Q29: Graph BFS DFS and Cycle Detection

## Problem Statement
Build a directed graph using adjacency list. Implement BFS, iterative DFS, recursive DFS, cycle detection (visited+recursion stack), and topological sort.

## Key Concepts
```
map[int][]int, visited set, DFS stack, Kahn's algorithm
```

## Requirements
- Solve the problem in `main.go`
- Include example output in comments
- Add unit tests where applicable

## How to Run
```bash
go run main.go
```
