# Q07: Once Singleton Pattern

## Problem Statement
Implement a thread-safe singleton database connection using sync.Once. Show what happens with a naive boolean flag. Demonstrate the race condition with the -race flag.

## Key Concepts
```
sync.Once, Do(), race condition demo
```

## Requirements
- Solve the problem in `main.go`
- Include example output in comments
- Add unit tests where applicable

## How to Run
```bash
go run main.go
```
