# Q36: Heap and Priority Queue

## Problem Statement
Implement a min-heap priority queue using container/heap interface (Len, Less, Swap, Push, Pop). Use it to merge K sorted arrays into one sorted array efficiently.

## Key Concepts
```
heap.Interface, heap.Init, heap.Push/Pop, K-way merge
```

## Requirements
- Solve the problem in `main.go`
- Include example output in comments
- Add unit tests where applicable

## How to Run
```bash
go run main.go
```
