# Q47: Struct Alignment and Memory Layout

## Problem Statement
Show how field ordering in structs affects memory size due to alignment padding. Write two versions of the same struct with different field orders. Use unsafe.Sizeof and unsafe.Offsetof to compare.

## Key Concepts
```
unsafe.Sizeof, unsafe.Alignof, unsafe.Offsetof, padding
```

## Requirements
- Solve the problem in `main.go`
- Include example output in comments
- Add unit tests where applicable

## How to Run
```bash
go run main.go
```
