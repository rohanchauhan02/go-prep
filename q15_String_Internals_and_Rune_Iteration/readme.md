# Q15: String Internals and Rune Iteration

## Problem Statement
Write a function that correctly reverses a UTF-8 string (not byte-by-byte). Count runes vs bytes. Demonstrate the difference between range on a string (yields runes) vs indexing (yields bytes).

## Key Concepts
```
[]rune, utf8.RuneCountInString, range string yields runes
```

## Requirements
- Solve the problem in `main.go`
- Include example output in comments
- Add unit tests where applicable

## How to Run
```bash
go run main.go
```
