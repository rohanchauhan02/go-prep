# Q46: String Builder Performance

## Problem Statement
Compare 4 string concatenation methods: + operator, fmt.Sprintf, bytes.Buffer, strings.Builder. Benchmark all four. Implement a CSV serializer using strings.Builder with minimal allocations.

## Key Concepts
```
strings.Builder, bytes.Buffer, testing.B, -benchmem flag
```

## Requirements
- Solve the problem in `main.go`
- Include example output in comments
- Add unit tests where applicable

## How to Run
```bash
go run main.go
```
