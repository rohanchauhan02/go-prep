# Q35: Interceptor Pattern

## Problem Statement
Implement a simple RPC dispatcher with unary interceptors (like gRPC UnaryServerInterceptor). Chain multiple interceptors: logging, auth, metrics. Show chaining via recursion/closures.

## Key Concepts
```
function types, closure chaining, variadic interceptors
```

## Requirements
- Solve the problem in `main.go`
- Include example output in comments
- Add unit tests where applicable

## How to Run
```bash
go run main.go
```
